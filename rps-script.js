// Player picks rock paper or scissors


// Computer picks rock paper or scissors randomly
function getComputerChoice(compRpsAnswer) {
    let rpsArray [                //Make an array of choices to pick
        "rock",
        "paper",
        "scissors"
    ];

    let = compRpsAnswer = rpsArray[Math.floor(Math.random()*rpsArray.length)]; //Pick a word from the array
}

// RPS answers are compared


// Display game results
